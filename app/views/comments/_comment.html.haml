.media
  .media-left
    = link_to image_tag(comment.user.avatar, alt: "64×64", class: "media-object", style: "width: 64px; height: 64px;"), user_path(comment.user_id)
  .media-body
    %h4#top-aligned-media.media-heading
      = link_to "#{comment.user.nickname}", user_path(comment.user_id)
      %span.anchorjs-icon
        %p
          = comment.text
